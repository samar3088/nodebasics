// In CommonJS all fiiles are modules by default
// Modules - Encapsulated Code (Only show the minimum)

const john = "john";
const peter = "peter";

const sayHi = (name) => {
  console.log(`Hello there ${name}`);
};

sayHi(john);
